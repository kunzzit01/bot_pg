# 复制为 .env 后填写真实值（.env 已被 .gitignore 忽略，不要提交到 git）
# BotFather 那里拿到的 token
BOT_TOKEN=123456789:AAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 数据目录（所有 ledger_*.json / operators.json 都存在这里）
# - Docker 部署：不用改。镜像里默认 /app/data，正好对应挂载的数据卷
# - 本地跑：取消下一行的注释，数据会落在当前目录的 data/
# BOT_DATA_DIR=./data
